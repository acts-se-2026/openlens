OPENROUTER_API_KEY=
YOLO_MODEL_PATH=

# Kratos public API used to validate session tokens (default shown; matches the local sandbox).
# KRATOS_PUBLIC_URL=http://localhost:4433

# Tokens granted to a wallet the first time a Kratos identity scans (default shown).
# STARTING_TOKENS=100
